diff --git a/src/TicTacToe.Console/Persistence/Csv/CsvWriter.cs b/src/TicTacToe.Console/Persistence/Csv/CsvWriter.cs
--- a/src/TicTacToe.Console/Persistence/Csv/CsvWriter.cs
+++ b/src/TicTacToe.Console/Persistence/Csv/CsvWriter.cs
@@ -41,23 +41,24 @@
 
         try
         {
-            using StreamWriter writer = new(
-                temporary_path,
-                append: false,
-                new UTF8Encoding(
-                    encoderShouldEmitUTF8Identifier: false));
+            using (StreamWriter writer = new(
+                       temporary_path,
+                       append: false,
+                       new UTF8Encoding(
+                           encoderShouldEmitUTF8Identifier: false)))
+            {
+                write_row(writer, header);
 
-            write_row(writer, header);
+                foreach (IReadOnlyList<string?> row in rows)
+                {
+                    if (row.Count != header.Count)
+                    {
+                        throw new InvalidOperationException(
+                            "Todas as linhas devem possuir a mesma quantidade de colunas do cabeçalho.");
+                    }
 
-            foreach (IReadOnlyList<string?> row in rows)
-            {
-                if (row.Count != header.Count)
-                {
-                    throw new InvalidOperationException(
-                        "Todas as linhas devem possuir a mesma quantidade de colunas do cabeçalho.");
+                    write_row(writer, row);
                 }
-
-                write_row(writer, row);
             }
 
             File.Move(
diff --git a/tests/TicTacToe.Tests/Persistence/Csv/CsvExportersTests.cs b/tests/TicTacToe.Tests/Persistence/Csv/CsvExportersTests.cs
--- a/tests/TicTacToe.Tests/Persistence/Csv/CsvExportersTests.cs
+++ b/tests/TicTacToe.Tests/Persistence/Csv/CsvExportersTests.cs
@@ -102,6 +102,30 @@
     }
 
     [Fact]
+    public void writer_should_close_temporary_file_before_replacement()
+    {
+        string path =
+            Path.Combine(directory, "replacement.csv");
+        CsvWriter writer = new();
+
+        writer.write(
+            path,
+            ["value"],
+            [["first"]]);
+
+        writer.write(
+            path,
+            ["value"],
+            [["second"]]);
+
+        Assert.Equal(
+            string.Join(
+                Environment.NewLine,
+                ["value", "second", string.Empty]),
+            File.ReadAllText(path));
+    }
+
+    [Fact]
     public void writer_should_use_utf8_without_bom()
     {
         string path = Path.Combine(directory, "utf8.csv");
